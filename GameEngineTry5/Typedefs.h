#pragma once
namespace EngineName
{
	typedef unsigned int td_width; //width of visibles inside engine
	typedef unsigned int td_height; //height of visible inside engine
	typedef double td_radius; //calculating width and height from a distance
	typedef double td_angle; //calculating widht and height from an angle
	typedef int td_xDirection; //relative position in x direction
	typedef int td_yDirection; //relative position in y direction
	typedef size_t td_ID; //id of objects
	typedef int td_scheduleTime; //storing how many ms a task will be scheduled with
}