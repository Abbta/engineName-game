#pragma once
namespace EngineName
{
	namespace Base
	{
		class ObjectContainer;
		/*
		* ObjectContainerAccess
		* Helper struct to ObjectContainer
		* Contains functions for accessing members of ObjectContainer
		*	but stores no variables in itself meaning it doesn't need
		*	to #include other files in order to be safe to #include in every other file
		*/
		struct ObjectContainerAccess
		{
			här, implementera klassen
		};
	}
}

